/** Label/value rows shown before the confirm card collapses the rest behind "show all". */
export const CONFIRM_VISIBLE_LINE_LIMIT = 12;

/** Rows shown by a list result card before it collapses behind "show all". */
export const RESULT_VISIBLE_ROW_LIMIT = 6;

/** Chat messages are denser to read, so a message card starts a little longer. */
export const MESSAGE_VISIBLE_ROW_LIMIT = 8;

/** Long chat messages are cut in the card; the model still received the full text. */
export const MESSAGE_TEXT_LIMIT = 500;
