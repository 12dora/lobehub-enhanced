/** i18n key prefix of this toolset inside the `plugin` namespace. */
export const DINGTALK_WORKSPACE_I18N_PREFIX = 'builtins.lobe-dingtalk-workspace' as const;

/** Label/value rows shown before the card collapses the rest behind "show all". */
export const CONFIRM_VISIBLE_LINE_LIMIT = 12;

/** Rows shown by a list result card before it collapses into `+N`. */
export const RESULT_VISIBLE_ROW_LIMIT = 6;
