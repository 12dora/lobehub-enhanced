export const ADMIN_AGENT_LIST_KEY = 'enterprise.admin.agents.list';

/** The pinned default assistant, read through the dedicated `isDefault` pointer lookup. */
export const ADMIN_AGENT_DEFAULT_KEY = 'enterprise.admin.agents.default';

/** The pinned 任务助手 card, read through the reserved `task-manager` agent key. */
export const ADMIN_AGENT_TASK_MANAGER_KEY = 'enterprise.admin.agents.taskManager';
